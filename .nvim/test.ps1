ForEach($line in $lines)
{
  # do stuff
  $test = Get-ChildItem -Recurse -Path -Name "Test"
}

ForEach($thing in $things)
{
  $Directory = Get-ChildItem -Recurse .
  Write-Host "Do Stuff" 

}
"String
